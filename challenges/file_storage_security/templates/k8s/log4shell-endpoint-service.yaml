apiVersion: v1
kind: Service
metadata:
  name: sudosingles-optimizer
  labels:
    app: log4shell
spec:
  selector:
    app: log4shell
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080